version: '3.8'

services:
  khamar-khata:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: khamar_khata_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL:-postgresql://shenron:abirmallik76922247@192.168.1.8:5432/main_db}
      - AUTH_SECRET=${AUTH_SECRET:-a8f93b7c2d1e0f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-https://abirmallik11.tail100a66.ts.net}
      - UPLOAD_DIR=/app/public/uploads
    volumes:
      - khamar_khata_uploads:/app/public/uploads

volumes:
  khamar_khata_uploads:
    driver: local
